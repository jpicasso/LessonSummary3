make sure everything is installed. below is how to install flask-cors

pip3 install -U flask-cors

to run program type

$ export FLASK_APP=flaskr
$ export FLASK_ENV=development
$ flask run

then in another terminal

$ clear 
$ curl http://127.0.0.1:5000/plants
$ curl 